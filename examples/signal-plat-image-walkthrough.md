# Signal Plat Image Pipe Walkthrough

This walk-through keeps the domain vocabulary close to the data instead of burying it in prose.

| Case | Focus | Score | Lane |
| --- | --- | ---: | --- |
| baseline | rollout width | 129 | watch |
| stress | quota pressure | 191 | ship |
| edge | route drift | 193 | ship |
| recovery | secret scope | 221 | ship |
| stale | rollout width | 124 | watch |

Start with `recovery` and `stale`. They create the widest contrast in this repository's fixture set, which makes them better review anchors than the middle cases.

The useful comparison is `secret scope` against `rollout width`, not the raw score alone.
